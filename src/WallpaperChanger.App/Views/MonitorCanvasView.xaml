<UserControl x:Class="WallpaperChanger.App.Views.MonitorCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WallpaperChanger.App.Views">
    <Border Padding="16" Background="#F4F6F8" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8">
        <ListBox Background="Transparent"
                 BorderThickness="0"
                 ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                  SelectedItem="{Binding SelectedMonitor, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                  KeyboardNavigation.DirectionalNavigation="Cycle"
                 ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                 ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <local:MonitorLayoutPanel />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                         <Setter Property="AutomationProperties.Name" Value="{Binding MonitorId, StringFormat=Monitor {0}}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="8"
                                Background="#FFFFFF"
                                BorderBrush="#64748B"
                                BorderThickness="2"
                                CornerRadius="6">
                            <Grid>
                                <Image x:Name="PreviewImage"
                                        Source="{Binding PreviewImagePath, NotifyOnTargetUpdated=True}"
                                        Stretch="Uniform"
                                        ImageFailed="PreviewImageFailed"
                                        TargetUpdated="PreviewImageSourceUpdated">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Source, ElementName=PreviewImage}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding (local:MonitorCanvasView.PreviewLoadFailed), ElementName=PreviewImage}" Value="True">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock x:Name="PreviewFallback"
                                           HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="#475569"
                                           Text="No preview available">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding Source, ElementName=PreviewImage}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                                <DataTrigger Binding="{Binding (local:MonitorCanvasView.PreviewLoadFailed), ElementName=PreviewImage}" Value="True">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                 <Rectangle IsHitTestVisible="False" StrokeThickness="4">
                                    <Rectangle.Style>
                                        <Style TargetType="Rectangle">
                                            <Setter Property="Stroke" Value="Transparent" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                    <Setter Property="Stroke" Value="#1D4ED8" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Rectangle.Style>
                                 </Rectangle>
                                 <Rectangle IsHitTestVisible="False" Stroke="#111827" StrokeThickness="3" StrokeDashArray="2,2" Visibility="Collapsed">
                                     <Rectangle.Style>
                                         <Style TargetType="Rectangle">
                                             <Setter Property="Visibility" Value="Collapsed" />
                                             <Style.Triggers>
                                                 <DataTrigger Binding="{Binding IsKeyboardFocusWithin, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                     <Setter Property="Visibility" Value="Visible" />
                                                 </DataTrigger>
                                             </Style.Triggers>
                                         </Style>
                                     </Rectangle.Style>
                                 </Rectangle>
                                 <Border VerticalAlignment="Bottom" Background="#CC0F172A" Padding="8,4">
                                     <StackPanel>
                                         <TextBlock Foreground="White" FontWeight="SemiBold" Text="{Binding MonitorId}" />
                                        <TextBlock Foreground="White">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Landscape" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPortrait}" Value="True">
                                                            <Setter Property="Text" Value="Portrait" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                         </TextBlock>
                                         <TextBlock FontWeight="Bold" Foreground="White" Text="Selected" Visibility="Collapsed">
                                             <TextBlock.Style>
                                                 <Style TargetType="TextBlock">
                                                     <Setter Property="Visibility" Value="Collapsed" />
                                                     <Style.Triggers>
                                                         <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                                             <Setter Property="Visibility" Value="Visible" />
                                                         </DataTrigger>
                                                     </Style.Triggers>
                                                 </Style>
                                             </TextBlock.Style>
                                         </TextBlock>
                                     </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
        </ListBox>
    </Border>
</UserControl>
