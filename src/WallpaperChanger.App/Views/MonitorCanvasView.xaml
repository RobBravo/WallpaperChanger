<UserControl x:Class="WallpaperChanger.App.Views.MonitorCanvasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:WallpaperChanger.App.Views">
    <UserControl.Resources>
        <local:NormalizedCoordinateConverter x:Key="NormalizedCoordinateConverter" />
        <local:LayoutHeightConverter x:Key="LayoutHeightConverter" />
    </UserControl.Resources>

    <Border Padding="16" Background="#F4F6F8" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8">
        <Viewbox Stretch="Uniform">
            <ListBox Width="1000"
                     Height="{Binding LayoutAspectRatio, RelativeSource={RelativeSource AncestorType=UserControl}, Converter={StaticResource LayoutHeightConverter}}"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding ItemsSource, RelativeSource={RelativeSource AncestorType=UserControl}}"
                     SelectedItem="{Binding SelectedMonitor, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                     ScrollViewer.VerticalScrollBarVisibility="Disabled">
                <ListBox.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ListBox.ItemsPanel>
                <ListBox.ItemContainerStyle>
                    <Style TargetType="ListBoxItem">
                        <Setter Property="Canvas.Left" Value="{Binding NormalizedLeft, Converter={StaticResource NormalizedCoordinateConverter}}" />
                        <Setter Property="Canvas.Top" Value="{Binding NormalizedTop, Converter={StaticResource NormalizedCoordinateConverter}}" />
                        <Setter Property="Width" Value="{Binding NormalizedWidth, Converter={StaticResource NormalizedCoordinateConverter}}" />
                        <Setter Property="Height" Value="{Binding NormalizedHeight, Converter={StaticResource NormalizedCoordinateConverter}}" />
                        <Setter Property="Padding" Value="0" />
                        <Setter Property="BorderThickness" Value="0" />
                        <Setter Property="Background" Value="Transparent" />
                        <Setter Property="AutomationProperties.Name" Value="{Binding MonitorId, StringFormat=Monitor {0}}" />
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="ListBoxItem">
                                    <ContentPresenter />
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </ListBox.ItemContainerStyle>
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="8"
                                Background="#FFFFFF"
                                CornerRadius="6">
                            <Border.Style>
                                <Style TargetType="Border">
                                    <Setter Property="BorderBrush" Value="#64748B" />
                                    <Setter Property="BorderThickness" Value="2" />
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ListBoxItem}}" Value="True">
                                            <Setter Property="BorderBrush" Value="#1D4ED8" />
                                            <Setter Property="BorderThickness" Value="6" />
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Border.Style>
                            <Grid>
                                <Image Source="{Binding CurrentImagePath}" Stretch="UniformToFill">
                                    <Image.Style>
                                        <Style TargetType="Image">
                                            <Setter Property="Visibility" Value="Visible" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CurrentImagePath}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Collapsed" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Image.Style>
                                </Image>
                                <TextBlock HorizontalAlignment="Center"
                                           VerticalAlignment="Center"
                                           Foreground="#475569"
                                           Text="No preview available">
                                    <TextBlock.Style>
                                        <Style TargetType="TextBlock">
                                            <Setter Property="Visibility" Value="Collapsed" />
                                            <Style.Triggers>
                                                <DataTrigger Binding="{Binding CurrentImagePath}" Value="{x:Null}">
                                                    <Setter Property="Visibility" Value="Visible" />
                                                </DataTrigger>
                                            </Style.Triggers>
                                        </Style>
                                    </TextBlock.Style>
                                </TextBlock>
                                <Border VerticalAlignment="Bottom" Background="#CC0F172A" Padding="8,4">
                                    <StackPanel>
                                        <TextBlock Foreground="White" FontWeight="SemiBold" Text="{Binding MonitorId}" />
                                        <TextBlock Foreground="White">
                                            <TextBlock.Style>
                                                <Style TargetType="TextBlock">
                                                    <Setter Property="Text" Value="Landscape" />
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding IsPortrait}" Value="True">
                                                            <Setter Property="Text" Value="Portrait" />
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </TextBlock.Style>
                                        </TextBlock>
                                    </StackPanel>
                                </Border>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Viewbox>
    </Border>
</UserControl>
